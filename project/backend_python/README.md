# backend_python

This folder is reserved for future Python backend logic (separate from the HTML/CSS/JS frontend).

Suggested next steps (optional):
- Create simple endpoints for products and checkout
- Protect secrets via environment variables
- Use a lightweight framework (Flask or FastAPI)

To run the sample Flask app below:

```bash
pip install -r requirements.txt
python app.py
```


